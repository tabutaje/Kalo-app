# Plantilla de credenciales de firma.
# Copia este archivo a `keystore.properties` y rellena los valores reales.
# El archivo real NUNCA se sube al repositorio (está en .gitignore).

storeFile=../../kalo-release-key.jks
storePassword=TU_PASSWORD_AQUI
keyAlias=TU_ALIAS_AQUI
keyPassword=TU_PASSWORD_AQUI
